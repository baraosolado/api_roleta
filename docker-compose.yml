services:
  roleta:
    build: .
    ports:
      - "8000:8000"
    restart: unless-stopped
    env_file:
      - .env
    environment:
      PYTHONUNBUFFERED: "1"
    volumes:
      - roleta_tmp:/tmp

volumes:
  roleta_tmp:
